import React from 'react';
import Camera from '../components/Camera'

const CameraScreen = ({navigation, route}) => {

    return <Camera navigation={navigation} route={route} />

}

export default CameraScreen;